# JobBook 版本编号守则

JobBook 使用 `主版本.功能版本.修复版本`，参考 [Semantic Versioning 2.0.0](https://semver.org/spec/v2.0.0.html)，但判断对象是用户可感知的产品兼容性，而不是营销上的“工作量大小”。

## 判定顺序

1. **主版本 `X.0.0`**：必须由用户明确确认，并且属于新的产品代际、核心工作流重构，或会让现有数据、备份、使用方式无法向后兼容。仅仅增加页面、榜单、导入方式或一组新功能，不自动构成主版本。
2. **功能版本 `X.Y.0`**：增加向后兼容的用户功能、扩展现有模块、增加可迁移的数据字段，或完成一组成体系的体验改进。
3. **修复版本 `X.Y.Z`**：修复错误、调整布局与文案、更新名单数据、改进性能、补充测试或文档，且不形成一组独立的新功能。
4. **预发布 `X.Y.Z-beta.N`**：尚未完成迁移或验收、只供测试的版本。预发布不得作为 GitHub Pages 默认稳定版。

主版本不能由实现者自行推断。没有用户明确确认时，即使改动很多，也只能在当前主版本内选择功能版本或修复版本。`src/releaseNotes.ts` 中的 `APPROVED_MAJOR_RELEASES` 是机器校验的主版本白名单。

## JobBook 的已确认主版本

- `1.0.0`：首个可用的 local-first JobBook。
- `2.0.0`：首次增加“职位发现”核心工作流。这是用户明确指定的大版本节点。

当前的公司名单、榜单搜索、截图 OCR 和批量刷新仍属于“职位发现”的兼容扩展，因此版本为 `2.1.0`，不是新的主版本。

## 历史编号校准

早期版本曾按“新增一级分区就升主版本”的规则编号，导致主版本增长过快。2026-09-23 按本守则一次性校准如下；功能内容、日期和数据迁移不变。

| 原编号 | 校准编号 | 内容 |
| --- | --- | --- |
| 1.0.0–1.4.0 | 不变 | 首个版本至简历模板资料区 |
| 2.0.0 | 1.5.0 | 我的简历工作台 |
| 2.1.0 | 1.6.0 | 职业地图 |
| 2.2.0 | 1.7.0 | 简历批注与白板 |
| 2.3.0 / 2.3.1 | 1.8.0 / 1.8.1 | 评论与标签管理 |
| 2.4.0 | 1.9.0 | 版本记录与备份目录 |
| 2.5.0 / 2.5.1 | 1.10.0 / 1.10.1 | 职业地图图层与网页版兼容 |
| 2.6.0 / 2.6.1 | 1.11.0 / 1.11.1 | PWA 与苹果设备支持 |
| 2.7.0 / 2.7.1 | 1.12.0 / 1.12.1 | OCR 与首页工作台 |
| 2.8.0–2.8.2 | 1.13.0–1.13.2 | 首页布局迭代 |
| 3.0.0 | 1.14.0 | 职业知识扩展 |
| 4.0.0 | 2.0.0 | 首次增加职位发现 |
| 5.0.0 | 2.1.0 | 公司名单、OCR 与批量刷新；纠正误升主版本 |

这些原编号未发布为依赖包 API，也没有参与 IndexedDB 架构版本判断；校准不会修改用户数据。Git 提交记录继续保留原始发布过程。

## 参考项目

- [VS Code 1.109 发布说明](https://github.com/microsoft/vscode-docs/blob/main/release-notes/v1_109.md)：同一功能版本包含大量显著功能，后续修复使用 `1.109.x`。
- [React 版本记录](https://github.com/reactjs/react.dev/blob/main/src/content/versions.md)：主版本用于代际，代内功能和维护分别使用 `19.1/19.2` 与 `19.2.x`。
- [Next.js 发布通道](https://github.com/vercel/next.js/blob/canary/contributing/repository/release-channels-publishing.md)：稳定通道按 SemVer，未稳定内容进入 `canary` 预发布通道。

## 发布检查

每次发布前运行：

```powershell
pnpm version:check
pnpm changelog
pnpm typecheck
pnpm test
```

发布版本还必须同步更新 `package.json`、`src/releaseNotes.ts`、`public/sw.js`、README 和 GitHub Pages 构建。已发布的稳定版本原则上不复用；本次属于建立规则时对连续一天内历史记录的一次性校准。
