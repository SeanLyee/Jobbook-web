# JobBook 版本编号守则

JobBook 使用 `主版本.功能版本.修复版本`。编号形式参考 [Semantic Versioning 2.0.0](https://semver.org/spec/v2.0.0.html)，但主版本按 JobBook 的产品功能代际判断，不要求先发生不兼容改动。

## 判定顺序

1. **主版本 `X.0.0`**：增加一个独立的大功能或新的长期工作流。通常会形成新的一级入口、核心数据对象或完整使用闭环。每个这样的功能都计为一个大版本，即使它与旧数据完全兼容。
2. **功能版本 `X.Y.0`**：在当前大功能内增加一组成体系的能力，例如新增导入方式、名单管理、批量操作或兼容支持，但没有形成另一个独立产品模块。
3. **修复版本 `X.Y.Z`**：修复错误，或调整布局、文案、数据、性能、测试和文档，不形成独立新功能。
4. **预发布 `X.Y.Z-beta.N`**：功能尚未完成迁移或验收，只用于测试；不得作为 GitHub Pages 默认稳定版。

工作量不是唯一判断标准：一项实现很复杂但只是在扩展现有模块，仍可属于功能版本；一个实现规模较小但建立了独立的核心工作流，也可以成为主版本。

发布新的 `X.0.0` 前，需要在更新日志中明确写出对应的大功能，并把版本加入 `src/releaseNotes.ts` 的 `APPROVED_MAJOR_RELEASES`。构建脚本会拒绝没有登记的大版本。

## JobBook 的大版本代际

- `1.0.0`：首个可用的 local-first 求职资料库。
- `2.0.0`：增加“我的简历”工作台，进入简历与职业规划阶段；后续职业地图、批注、PWA 和首页工作台属于这一代的持续扩展。
- `3.0.0`：增加职业知识词条、资料摘录、地图图层与思维导图工作流。
- `4.0.0`：增加“职位发现”，建立公司名单、公开岗位、匹配与投递状态工作流。

当前的榜单搜索、截图 OCR 导入、公司名单和批量刷新都在扩展“职位发现”，因此当前版本为 `4.1.0`，不是 `5.0.0`。

## 历史编号恢复

2026-09-23 曾短暂按严格兼容性规则把 `2.x–4.x` 压缩为 `1.x–2.x`。该规则不符合 JobBook “每个大功能算一个大版本”的产品定义，现已恢复原有功能代际；功能内容、发布日期和用户数据均未改变。

| 正式编号 | 短暂错误编号 | 内容 |
| --- | --- | --- |
| 1.0.0–1.4.0 | 不变 | 首个版本至简历模板资料区 |
| 2.0.0 | 1.5.0 | 我的简历工作台，第二个产品代际 |
| 2.1.0–2.8.2 | 1.6.0–1.13.2 | 职业地图、批注、备份、PWA、OCR 与首页扩展 |
| 3.0.0 | 1.14.0 | 职业知识词条、摘录、图层与思维导图 |
| 4.0.0 | 2.0.0 | 职位发现 |
| 4.1.0 | 2.1.0（最初误标为 5.0.0） | 公司名单、榜单搜索、OCR 与批量刷新 |

这些编号不参与 IndexedDB 架构版本判断，恢复编号不会修改或清除用户资料。Git 提交记录会继续保留编号纠正过程。

## 参考项目与 JobBook 的取舍

- [VS Code 1.109 发布说明](https://github.com/microsoft/vscode-docs/blob/main/release-notes/v1_109.md)：同一功能版本包含大量显著功能，后续修复使用 `1.109.x`。
- [React 版本记录](https://github.com/reactjs/react.dev/blob/main/src/content/versions.md)：主版本用于代际，代内功能和维护分别使用 `19.1/19.2` 与 `19.2.x`。
- [Next.js 发布通道](https://github.com/vercel/next.js/blob/canary/contributing/repository/release-channels-publishing.md)：稳定通道按 SemVer，未稳定内容进入 `canary` 预发布通道。

JobBook 借用三段式编号和预发布方式，但对“主版本”的定义以用户确认的产品大功能为准。

## 发布检查

每次发布前运行：

```powershell
pnpm version:check
pnpm changelog
pnpm typecheck
pnpm test
```

发布版本还必须同步更新 `package.json`、`src/releaseNotes.ts`、`public/sw.js`、README 和 GitHub Pages 构建。已经发布的稳定版本不复用。
